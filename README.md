ack-pack
=========

A pack to set up 'ack' interface to emacs from https://github.com/leoliu/ack-el

# install

In your `.emacs-live.el` add this snippet:
```elisp
(live-add-packs '(/path/to/ack-pack))
```
